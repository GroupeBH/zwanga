import { styles as containerStyles } from './container.styles';
import { styles as submitButtonDisabledStyles } from './submitButtonDisabled.styles';

export const styles = {
  ...containerStyles,
  ...submitButtonDisabledStyles,
};
