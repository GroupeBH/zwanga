import { styles as containerStyles } from './container.styles';
import { styles as primaryButtonTextStyles } from './primaryButtonText.styles';

export const styles = {
  ...containerStyles,
  ...primaryButtonTextStyles,
};
