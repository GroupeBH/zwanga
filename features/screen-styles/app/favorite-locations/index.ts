import { styles as containerStyles } from './container.styles';
import { styles as saveButtonStyles } from './saveButton.styles';

export const styles = {
  ...containerStyles,
  ...saveButtonStyles,
};
