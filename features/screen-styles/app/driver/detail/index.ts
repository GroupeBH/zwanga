import { styles as backButtonTextStyles } from './backButtonText.styles';
import { styles as containerStyles } from './container.styles';

export const styles = {
  ...containerStyles,
  ...backButtonTextStyles,
};
